# Config local del workspace (auto-aprobación de MCPs, etc.) — no se versiona
.claude/settings.local.json

# Dependencias (por si se agrega tooling node)
node_modules/

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Python (el repo tiene scripts de skill; el venv vive fuera, en ~/.indash/)
__pycache__/
*.pyc
